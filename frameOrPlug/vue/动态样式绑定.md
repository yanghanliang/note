#### vue 的排他功能

```html

<div v-for="(item,index) in items" :class="[isactive === index ? 'addclass' : '']" @click='onclick(index)'>
    <span>{{item.name}}</span>
</div>

```

```js

onclick(index){
    //将点击的元素的索引赋值给bian变量
    this.isactive = index
}

```